// '목록으로' 버튼 클릭 시 이전 페이지로 이동하는 함수
document.querySelector('.back-btn').addEventListener('click', () => {
    window.history.back();
});

// Mock 데이터를 사용해 회사 정보를 보여주는 로직
document.addEventListener('DOMContentLoaded', () => {
    
});
